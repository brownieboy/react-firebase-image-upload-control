/* eslint-env browser */
import React from "react";
import PropTypes from "prop-types";

const myModule = () => {
  return React.createElement("span", null, "This is my module");
};

export default myModule;
